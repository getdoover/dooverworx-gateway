# port 1194
proto tcp-server
dev tap0

; Static key mode (NO CERTS)
secret /etc/openvpn/static.key

; Keep it stable
persist-key
persist-tun
verb 4
mute 10

; Choose modern-ish crypto knobs (OpenVPN will accept these in most builds)
auth SHA256
cipher AES-256-CBC
